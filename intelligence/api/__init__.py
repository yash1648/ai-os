"""
API layer — HTTP route definitions for the PIL sidecar.

Mounts query endpoints exposed to the kernel, planner, and external tools.
All routes are registered on the FastAPI app via APIRouter.
"""
